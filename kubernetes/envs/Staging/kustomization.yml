resources:
  - ../../base
  - secrets/app.secret.yml
  - scaler.yml
  - secrets/minio-secret.secret.yaml
  - secrets/keys.secret.yml
  - secrets/test-keys.secret.yml

generatorOptions:
    disableNameSuffixHash: false

configMapGenerator:
- name: env
  namespace: ${NAMESPACE}
  behavior: merge
  envs:
    - .env
